# .env.example  —  Copy to .env and fill in your values
# ─────────────────────────────────────────────────────

# ── OpenAI (set LLM_PROVIDER=openai in config.py to use) ──
OPENAI_API_KEY=sk-...

# ── Pinecone ──────────────────────────────────────────
PINECONE_API_KEY=pcsk_...
PINECONE_INDEX_NAME=priya-rag-index

# ── Persistence paths (optional — defaults to project dir) ──
# DB_PATH=/var/data/sessions.db
# CORPUS_PATH=/var/data/bm25_corpus.json

# ── Security ──────────────────────────────────────────
# Set a strong random string to enable API key auth.
# Leave blank to disable (development mode).
# API_KEY=your-secret-key-here

# ── CORS ─────────────────────────────────────────────
# Comma-separated list of allowed frontend origins.
# Use * only in development.
# CORS_ORIGINS=https://yourapp.com,https://www.yourapp.com
CORS_ORIGINS=*

# ── Rate limiting ─────────────────────────────────────
# Format: "N/period"  (periods: second, minute, hour, day)
RATE_LIMIT=30/minute

# ── Logging ───────────────────────────────────────────
LOG_LEVEL=INFO
