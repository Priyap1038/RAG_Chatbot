# .env.example for rag_frontend
# Copy to .env (development) or .env.production (production build)

# ── API Base URL ───────────────────────────────────────
# Leave empty in development (Vite proxy handles /api/* → localhost:8000)
# Set to your backend URL in production:
# VITE_API_URL=https://api.yourapp.com
VITE_API_URL=

# ── API Key (optional) ────────────────────────────────
# Set if API_KEY is configured in the backend .env
# VITE_API_KEY=your-secret-key-here
VITE_API_KEY=
